Legacy snapshots and manifests kept for reference.

These files are not used by the current runtime service:
- `main.py` is the only active FastAPI app entrypoint.
- `requirements.txt` is the active dependency manifest.

Contents in this folder:
- old app snapshots (`main V1 copy.py`, `main V2 con API - copy.py`)
- historical dependency exports (`req.txt`, `req2.txt`)
